{
  "//": "Manual MCP wire for Claude Code without the plugin. Prefer the plugin (../../plugin/) — it auto-resolves the namespace from the agent's cwd. If you must wire by hand, replace 'my-project' with your real namespace or drop the X-Memini-Namespace header and let the server auto-resolve.",
  "mcpServers": {
    "memini": {
      "type": "http",
      "url": "http://localhost:8080/mcp",
      "headers": {
        "X-Memini-Namespace": "my-project"
      }
    }
  }
}

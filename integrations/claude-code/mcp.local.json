{
  "//": "Manual stdio wire for Claude Code without the plugin. Prefer the plugin (../../plugin/) — it auto-resolves the namespace from the agent's cwd. If you must wire by hand, replace 'my-project' with your real namespace or drop MEMINI_DEFAULT_NAMESPACE and let the server auto-resolve from its own cwd.",
  "mcpServers": {
    "memini": {
      "type": "stdio",
      "command": "memini",
      "args": ["mcp"],
      "env": {
        "MEMINI_DEFAULT_NAMESPACE": "my-project"
      }
    }
  }
}

/bin/
/dist/
# UI dependencies. The built bundle (internal/api/ui/dist) IS committed so the
# Go binary always embeds it; node_modules is not.
/ui/node_modules/
*.db
*.db-shm
*.db-wal
memini.db*
/tmp/
cover.out
coverage.out
.env
# Benchmark run outputs (generated; not part of the repo).
/bench/results/
# Local agent session notes.
/.remember/

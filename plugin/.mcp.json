{
  "//": "Auto-loaded by Claude Code from the plugin root. Registers the memini MCP server (memory_remember/recall/get/forget). Works local AND remote: set MEMINI_MCP_URL to your remote memini's /mcp URL and MEMINI_TOKEN to its API key. The headersHelper resolves the per-project namespace from CLAUDE_PROJECT_DIR (same resolver as the hooks) and adds the bearer token, so one remote server serves every project correctly.",
  "mcpServers": {
    "memini": {
      "type": "http",
      "url": "${MEMINI_MCP_URL:-http://localhost:8080/mcp}",
      "headersHelper": "node ${CLAUDE_PLUGIN_ROOT}/scripts/mcp-headers.mjs"
    }
  }
}

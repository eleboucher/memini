{
  "name": "@memini/namespace-resolver",
  "version": "0.1.0",
  "description": "Shared namespace resolver for memini integrations — tenant roots, project, and agent segments with configurable templates.",
  "license": "MIT",
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm exec tsc -p tsconfig.test.json && node --test dist/test/resolver.test.js",
    "typecheck": "tsc -p tsconfig.test.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^7.0.0"
  }
}

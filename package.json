{
  "name": "@memini/workspace",
  "private": true,
  "description": "pnpm workspace root for shared client packages and the plugins that consume them; Rust and standalone integrations are not part of the workspace."
}
